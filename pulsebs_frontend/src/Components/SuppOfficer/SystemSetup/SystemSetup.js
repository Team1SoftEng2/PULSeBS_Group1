import React from 'react';

function systemSetup() {
    return (
        <div>
            Here the Support Officer will setup the system, uploading courses, students and professors
        </div>
    );
}

export default systemSetup;