import React from 'react';

function systemUpdate() {
    return (
        <div>
            Here the support officer will update the bookable lecture.
        </div>
    );
}

export default systemUpdate;