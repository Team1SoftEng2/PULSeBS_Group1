import React from 'react';

function myLecturesProfessor() {
    return (
        <div>
            This is where the professor checks the lessons, cancels them or changes them from in person to online.
        </div>
    );
}

export default myLecturesProfessor;