import React from 'react';

function registerAttendance() {
    return (
        <div>
            Here the professor will register the students attending from the ones who booked the lesson
        </div>
    );
}

export default registerAttendance;